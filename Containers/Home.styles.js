import { StyleSheet } from 'react-native';

const style =  StyleSheet.create({
    HomeWrapper:{
        margin: 0,
        padding: 0,
        // flex:1,
        // alignItems: 'center',
        // justifyContent: 'center',
       
    },
    TextWrapper:{
        color: 'red',
    }
});

export { style }; 